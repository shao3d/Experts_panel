# Gemini CLI: Автономный Drift Analysis (20 групп)

**Копируй этот промпт в Gemini CLI. Он обработает все 20 групп автоматически.**

---

## 🎯 Задача

Проанализируй **20 pending групп** комментариев на "дрифт" — отклонение от темы поста. **Работай АВТОНОМНО**, без пауз между группами.

**База:** `backend/data/experts.db`

---

## � ВЫПОЛНИ ВСЁ АВТОМАТИЧЕСКИ:

### 1. Получи 20 pending групп:
```bash
sqlite3 backend/data/experts.db "SELECT post_id FROM comment_group_drift WHERE analyzed_by = 'pending' LIMIT 20;"
```

### 2. Для КАЖДОГО post_id из списка (без пауз!):

**A. Читай данные:**
```bash
sqlite3 backend/data/experts.db "SELECT message_text FROM posts WHERE post_id = <ID>;" && sqlite3 backend/data/experts.db "SELECT comment_text FROM comments WHERE post_id = <ID> LIMIT 30;"
```

**B. Анализируй:**
- О чём пост? О чём комменты?
- **Дрифт = существенное отклонение** (новые темы, оффтоп, споры не по теме)
- **НЕ дрифт** = уточнения, согласие, благодарности

**C. Записывай результат:**

Дрифт ЕСТЬ:
```bash
sqlite3 backend/data/experts.db "UPDATE comment_group_drift SET has_drift = 1, drift_topics = '{\"has_drift\":true,\"drift_topics\":[{\"topic\":\"<ТЕМА>\",\"keywords\":[\"<слова>\"],\"key_phrases\":[\"<цитата>\"],\"context\":\"<почему дрифт>\"}]}', analyzed_by = 'gemini-3-ai.dev', analyzed_at = datetime('now') WHERE post_id = <ID>;"
```

Дрифта НЕТ:
```bash
sqlite3 backend/data/experts.db "UPDATE comment_group_drift SET has_drift = 0, drift_topics = NULL, analyzed_by = 'gemini-3-ai.dev', analyzed_at = datetime('now') WHERE post_id = <ID>;"
```

### 3. После всех 20 — покажи итог:
```
✅ Обработано: X групп
   - С дрифтом: Y
   - Без дрифта: Z
```

---

## ⚠️ Правила

- **Автономно** — не жди подтверждений, обрабатывай все 20 подряд
- **Реальные данные** — читай из базы, не выдумывай
- **JSON в одну строку** — экранируй кавычки
- **Метка:** `analyzed_by = 'gemini-3-ai.dev'`

---

**СТАРТ! Получи список и обработай все 20 групп.**
